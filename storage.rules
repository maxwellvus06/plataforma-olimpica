rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /plataforma/{allPaths=**} {
      allow read, write: if true;
    }
    match /layout/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
